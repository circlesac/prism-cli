# Circles credentials for Go

```go
import credentials "github.com/circlesac/credentials/go"
```

This package implements the shared contract in the repository root and runs the
same fixtures as the Node implementation.
